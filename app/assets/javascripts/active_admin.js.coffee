#= require active_admin/base

jQuery ->
  # words
  # $('.word_subtheme').each ->
  #   $(this).parent().hide()
  #   $(this).parent().attr('id', '')

  # $('#word_theme').on 'change', (event) ->
  #   $('.word_subtheme').each ->
  #     $(this).parent().hide()
  #     $(this).parent().attr('id', '')
  #   $("#word_subtheme.#{event.target.value}").parent().show().attr('id', 'word_subtheme_input')

  # $('#word_subtheme_input #word_subtheme').on 'change', (event) ->
  #   console.log this

  # # verbs
  # $('.verb_subtheme').each ->
  #   $(this).parent().hide()

  # $('#verb_theme').on 'change', (event) ->
  #   $('.verb_subtheme').each ->
  #     $(this).parent().hide()
  #   $("#verb_subtheme.#{event.target.value}").parent().show()

  # #phrases
  # $('.phrase_subtheme').each ->
  #   $(this).parent().hide()

  # $('#phrase_theme').on 'change', (event) ->
  #   $('.phrase_subtheme').each ->
  #     $(this).parent().hide()
  #   $("#phrase_subtheme.#{event.target.value}").parent().show()
