<%= form_for(api_carrusel) do |f| %>
  <% if api_carrusel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_carrusel.errors.count, "error") %> prohibited this api_carrusel from being saved:</h2>

      <ul>
      <% api_carrusel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
