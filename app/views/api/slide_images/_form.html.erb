<%= form_for(api_slide_image) do |f| %>
  <% if api_slide_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_slide_image.errors.count, "error") %> prohibited this api_slide_image from being saved:</h2>

      <ul>
      <% api_slide_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
