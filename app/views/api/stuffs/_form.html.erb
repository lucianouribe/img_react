<%= form_for(api_stuff) do |f| %>
  <% if api_stuff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_stuff.errors.count, "error") %> prohibited this api_stuff from being saved:</h2>

      <ul>
      <% api_stuff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
